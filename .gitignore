# runtime / generated databases (carry seeded vault content — never commit)
*.db
*.db-journal
*.sqlite
__pycache__/
*.pyc

# build orchestration (internal scaffolding, machine-specific paths)
_build/

# adversarial security review reports (internal audit artifacts — don't ship)
_review/

# internal planning artifacts (design/plan docs quote homelab specifics — never ship)
docs/

# distill artifacts — regenerate per-machine via `distill-plan` / `export-training`
training.jsonl
distill_config.json
distill.ps1

# weights PoC work dir — 47GB of GGUFs, vendored llama.cpp, adapters, scratch DBs (machine-specific)
_weights/

# packaging — throwaway install-verification venv + build artifacts
_pkgtest_venv/
dist/
*.egg-info/

# personal runtime artifacts — never commit. The forgetting/recall/episodic layers
# emit these locally; the generated brain index and config backups carry user data.
*.log
*.bak
secret_denylist.txt
MEMORY.md
MEMORY.memsom.md
.weights/
# (episodic engine stores, belt-and-suspenders on top of *.db above)
sessions.db
vault*.db
mem_weights.db

# personal MCP-BGE launcher (real paths/username) — never ship
mcp-bge.bat
